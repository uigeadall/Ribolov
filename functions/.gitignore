# Compiled TypeScript output (regenerated by `npm run build`)
# Anchored so it ignores only functions/lib/ (the tsc outDir), NOT source
# folders like functions/src/lib/.
/lib/

# Installed dependencies (regenerated by `npm install`)
node_modules/

# Firebase deploy logs
*.log
